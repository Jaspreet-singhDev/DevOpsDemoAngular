import { DividePipePipe } from './divide-pipe.pipe';

describe('DividePipePipe', () => {
  it('create an instance', () => {
    const pipe = new DividePipePipe();
    expect(pipe).toBeTruthy();
  });
});
